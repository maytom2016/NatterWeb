name: PyInstaller Windows Build

on:
  workflow_dispatch:

jobs:
  windows-build:
    runs-on: windows-latest  # 使用 Windows 运行器

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          architecture: 'x64'  # 明确指定64位架构

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build with PyInstaller (Windows)
        run: |
          pyinstaller `
            --name=app `
            --onefile `
            --add-data="shared_vars.py;." `
            --add-data="plugin;plugin" `
            --add-data="static;static" `
            --add-data="templates;templates" `
            --hidden-import=plugin.notification.pg `
            --console  # Windows 建议显式指定控制台/窗口模式
            app.py

      - name: Verify Windows build
        run: |
          dir dist\app.exe
          strings dist\app.exe | Select-String "pg.py"  # PowerShell 版本的 strings 检查

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-windows
          path: dist\app.exe
          retention-days: 7
